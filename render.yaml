services:
  - type: web
    name: dcinside-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run news_dashboard.py --server.port=10000 --server.address=0.0.0.0
